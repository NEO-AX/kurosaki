# SUPERVISOR.md —— 監査法人の権限境界

本書はこのリポジトリのすべての文書より上位に立つ。

## 誰が何を変えられるか

| 対象 | 変更できる者 |
|------|--------------|
| 本書 | 所有者（人間）のみ |
| 検出手続（`kurosaki/checks/`）・検出ルール（`rules.py` / `structure.py`）の**緩和** | 所有者の明示承認を得た場合のみ |
| 検出手続の追加・誤検知の修正（検出が残ることをテストで示せる場合） | 実装担当（AI含む）が提案し、テストが通れば可 |
| `templates/` の雛形 | 実装担当が提案可。ただし雛形の緩和は上と同じ扱い |
| 被監査リポジトリへの `install` の実行 | 所有者のみ（対象リポジトリへ書き込む操作である） |

## 監査法人自身が監査される

この道具は自分自身にも適用する。`kurosaki audit --repo .` で Critical / High が
出ている状態を「完成」と呼ばない。自分に甘い設計をした時点で、成果物は無価値になる。

## 記録

意見・所見・残存リスクは `.audit/reports/` と `docs/BYPASS_DRILL.md` に残す。
過去の記録を書き換えない（`D7-03` が検知する）。
