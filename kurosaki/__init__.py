"""金融庁の黒崎 —— 独立監査ツール。

このパッケージは対象リポジトリの外に置かれる。実装セッションのAIが
触れるのは対象リポジトリだけなので、検出ロジックの正本がここにある限り
「監査を通しやすくする改変」は対象リポジトリ側からは届かない（P2）。

標準ライブラリのみで動く。外部依存は「あれば使う」に限り、無い場合は
検出を緩めるのではなく、緩和を無効にして FAIL 側へ倒す。
"""

__version__ = "0.1.0"
